Ось покроковий опис бізнес-логіки та процесу доставки посилки в твоєму бекенді, починаючи від її створення користувачем і закінчуючи отриманням. Включаю всі дійові особи: **користувач (відправник/отримувач), адміністратор, кур'єр**.

---

## **1. Створення посилки користувачем (відправником)**
🔹 **Що робить користувач?**  
- Відправник створює нову посилку через ендпоінт **`POST /shipment/`**  
- Вказує дані: 
  - `receiver_id` – кому адресована посилка  
  - `branch_from` – відділення, звідки відправляється  
  - `branch_to` – відділення, куди має надійти  
  - `weight`, `length`, `width` – параметри посилки  
  - `price` – поки що не розраховується  

🔹 **Що відбувається в бекенді?**  
1. Генерується **унікальний трек-номер** (наприклад, `create_tracking_number()`).
2. Валідується вхідна інформація:
   - Чи авторизований користувач?
   - Чи не намагається він відправити самому собі?
3. Створюється запис у таблиці **Shipment** із початковим статусом `"created"`.
4. Додається запис у таблицю **ShipmentStatus** (статуси відстеження).

---

## **2. Оплата посилки (опціонально)**
🔹 **Що робить користувач?**  
- Відправник (або отримувач, залежно від логіки) оплачує доставку через **`POST /payment/{shipment_id}`**  

🔹 **Що відбувається в бекенді?**  
1. Перевіряється, чи така посилка існує.  
2. Перевіряється, чи вже є оплата.  
3. Створюється запис у **Payment** з деталями оплати.  
4. Статус посилки змінюється на `"paid"`.  

---

## **3. Прийом посилки на відділенні відправлення (адміністратор)**
🔹 **Що робить адміністратор?**  
- Приймає посилку у відділенні, підтверджує її відправлення.
- Через **`PUT /shipment/change_status/{tracking_number}`** змінює статус посилки на `"accepted"`.

🔹 **Що відбувається в бекенді?**  
1. Перевіряється, чи посилка оплачена.  
2. Якщо так – змінюється статус на `"accepted"`.  
3. Додається запис у **ShipmentStatus**.  

---

## **4. Доставка посилки між відділеннями (кур'єр)**
🔹 **Що робить кур'єр?**  
- Отримує список посилок на доставку через **`GET /shipment/from_me`**.  
- Забирає посилки з відділення-відправника.  
- Через **`PUT /shipment/change_status/{tracking_number}`** змінює статус на `"in_transit"`.

🔹 **Що відбувається в бекенді?**  
1. Перевіряється, чи користувач – **кур'єр**.  
2. Оновлюється статус посилки на `"in_transit"` (в дорозі).  
3. Додається запис у **ShipmentStatus**.  

---

## **5. Доставлення посилки в пункт призначення (адміністратор)**
🔹 **Що робить адміністратор?**  
- Приймає посилку у відділенні отримувача.  
- Через **`PUT /shipment/change_status/{tracking_number}`** змінює статус на `"arrived_at_branch"`.  

🔹 **Що відбувається в бекенді?**  
1. Перевіряється, чи посилка була в статусі `"in_transit"`.  
2. Якщо так – статус змінюється на `"arrived_at_branch"`.  
3. Додається запис у **ShipmentStatus**.  

---

## **6. Доставка до дверей отримувача (кур'єр)**
🔹 **Що робить кур'єр?**  
- Бере посилку на доставку (отримує її через **`GET /shipment/me`**).  
- Через **`PUT /shipment/change_status/{tracking_number}`** змінює статус на `"out_for_delivery"`.

🔹 **Що відбувається в бекенді?**  
1. Перевіряється, чи посилка прибула у відділення.  
2. Якщо так – статус змінюється на `"out_for_delivery"` (вийшла на доставку).  
3. Додається запис у **ShipmentStatus**.  

---

## **7. Отримання посилки користувачем**
🔹 **Що робить отримувач?**  
- Приходить у відділення або приймає посилку від кур'єра.  
- Кур'єр або адміністратор змінює статус через **`PUT /shipment/change_status/{tracking_number}`** на `"delivered"`.

🔹 **Що відбувається в бекенді?**  
1. Перевіряється, чи посилка була у статусі `"out_for_delivery"`.  
2. Якщо так – статус змінюється на `"delivered"`.  
3. Додається запис у **ShipmentStatus**.  

---

## **8. Перевірка статусу посилки в будь-який момент**
🔹 **Що робить користувач?**  
- Перевіряє статус за **`GET /shipment/{tracking_number}`**.  

🔹 **Що відбувається в бекенді?**  
1. Шукається посилка за її трек-номером.  
2. Віддається вся інформація про неї, включаючи її статус.  

---

## **9. Видалення посилки (може зробити тільки відправник)**
🔹 **Що робить відправник?**  
- Через **`DELETE /shipment/{tracking_number}`** видаляє посилку, якщо вона ще не в дорозі.  

🔹 **Що відбувається в бекенді?**  
1. Перевіряється, чи користувач – **відправник** цієї посилки.  
2. Перевіряється, чи вона ще **не в статусі `"in_transit"`**.  
3. Видаляється запис у **Shipment**.  

---

## **Короткий підсумок ролей**
| Роль           | Дії |
|---------------|------------------------------------------------------------|
| **Користувач (відправник)** | Створює посилку, оплачує, перевіряє статус, може видалити її |
| **Користувач (отримувач)** | Чекає доставку, перевіряє статус, приймає посилку |
| **Адміністратор** | Приймає посилки у відділеннях, керує їх статусами |
| **Кур'єр** | Доставляє між відділеннями, доставляє до дверей отримувача |

---

Якщо треба доповнити або змінити логіку – питай! 😃